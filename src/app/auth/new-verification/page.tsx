import { NewVerificationCard } from "@/components/auth/new-verification-card";

const NewVerificationPage = () => {
  return (
    <div className="w-full h-screen flex items-center justify-center bg-gradient-to-tr from-red-700 to-orange-400">
      <NewVerificationCard />
    </div>
  );
};

export default NewVerificationPage;
